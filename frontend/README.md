# Ninho frontend

Frontend Alpha em React, Vite e TypeScript.

```powershell
npm install
npm run dev
```

A API é lida de `VITE_API_BASE_URL`; em desenvolvimento o fallback é `http://localhost:8080/api`.
